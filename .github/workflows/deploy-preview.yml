name: Temporary V70 visual preview

on:
  push:
    branches:
      - preview/v70-340
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-v70-340-preview
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact integration commit
        uses: actions/checkout@v4
        with:
          ref: 7bb7df17dacfcbb0205cc7bd7e11db5c3f75287a
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build -- --base=/radar-platform/
      - name: Adapt absolute public assets for project preview
        run: |
          sed -i 's|/brand/|/radar-platform/brand/|g' dist/assets/*.js
          cp dist/index.html dist/404.html
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: v70-preview
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy isolated visual preview
        id: deployment
        uses: actions/deploy-pages@v4
